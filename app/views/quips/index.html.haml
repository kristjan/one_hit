- content_for :header do
  = stylesheet_link_tag "quips/new"
%ul
  - @quips.each do |quip|
    %Li
      %span= quip.text
      = link_to 'Delete', site_quip_path(@site, quip), :method => :delete
= render 'form'
